    <section class="hero">

    <div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,168,68,0.6) 0%, rgba(0,61,36,0.6) 50%), url('<%= image_path('background.jpeg') %>');">
      <div class="banner-content">
        <h1>FanBased</h1>
        <p>Meet likeminded fans wherever you are</p>
      </div>
    </div>
</section>

    <section class="content">
      <div class="text-center">
        <% if current_user %>
        <ul style="list-style: none;">
          <h3>Your Teams</h3>
          <% current_user.team.each do |team| %>
          <%= link_to team_path(team) do %>
          <h3><li> <%= team.name %> </li></h3>
          <% end %>
          <% end %>
        </ul>
        <% else %>
        <div class="container padded text-center">
          <div class="row">
            <div class="about">
              <div class="col-xs-12 col-sm-4 ">
                <%= image_tag("soccer-icon.png")%>
                <p>something cleaver about football</p>

              </div>
              <div class="col-xs-12 col-sm-4">
                <%= image_tag("t-shirt-icon.png")%>
                <p>something cleaver about watching the game with people from the same team</p>
              </div>
              <div class="col-xs-12 col-sm-4">
                <%= image_tag("trophy-icon.png")%>
                <p>Something cleaver about everything</p>
              </div>
            </div>
          </div>
        </div>
        <% end %>
      </div>



      <div class="venues">
        <div class="container padded text-center">
          <div class="row">
            <div class="col-xs-12 col-sm-12 ">
              <h2>More than <strong> 30000 VENUES</strong> with the best enviroment for you to enjoy the game just like in the stadium and more shit tha is going to be good trust me.</h2>
            </div>
          </div>
        </div>
      </div>


  <div class="map">
    <div id="map" style="width: 100%; height: 600px;"></div>
  </div>


      <%= render 'shared/footer' %>


<% content_for(:after_js) do %>
<%= javascript_tag do %>
$(document).ready(function() {
var handler = Gmaps.build('Google');
handler.buildMap({ internal: { id: 'map' } }, function() {
markers = handler.addMarkers(<%= raw @hash.to_json %>);
handler.bounds.extendWith(markers);
handler.fitMapToBounds();
if (markers.length == 0) {
handler.getMap().setZoom(2);
} else if (markers.length == 1) {
handler.getMap().setZoom(14);
}
});
});
<% end %>
<% end %>




